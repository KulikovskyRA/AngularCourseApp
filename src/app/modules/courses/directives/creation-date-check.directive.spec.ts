import { CreationDateCheckDirective } from './creation-date-check.directive';

describe('CreationDateCheckDirective', () => {
  it('should create an instance', () => {
    const directive = new CreationDateCheckDirective();
    expect(directive).toBeTruthy();
  });
});
